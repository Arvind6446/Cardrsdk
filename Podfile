# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Cardr SDK' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Cardr SDK
pod 'Alamofire-SwiftyJSON'

  target 'Cardr SDKTests' do
    # Pods for testing
  end

end
